# 💰 Personal Finance Manager (CLI)

## 📌 Overview
A Python-based Command Line Interface (CLI) application designed to track personal expenses, manage budgets, and visualize spending habits. This project demonstrates core Computer Science principles including **CRUD operations**, **Data Persistence**, and **Algorithm implementation** for data aggregation.

## 🛠 Tech Stack
- **Language:** Python 3.x
- **Data Storage:** JSON (File I/O for lightweight persistence)
- **Architecture:** Modular implementation with Object-Oriented Programming (OOP)

## ✨ Key Features
- **Data Persistence:** Automatically saves and loads transaction data using JSON, ensuring no data loss between sessions.
- **Expense Tracking:** Allows users to input expense categories, amounts, and descriptions.
- **Financial Analytics:** Algorithms to aggregate data and calculate total spending by category (e.g., Food, Transport).
- **Input Validation:** Robust error handling to prevent system crashes from invalid user inputs.

## 🚀 How to Run
1. Ensure Python is installed.
2. Clone this repository or download `finance_tracker.py`.
3. Run the application:
   ```bash
   python finance_tracker.py# Personal-Finance-Manage
